You are a senior software engineer tasked with reframing and repositioning an existing iOS app called "The Oracle" — a palm reading app built in Expo/React Native — into a premium AI life advisor app. The palm reading functionality stays intact and working. We are not rebuilding anything from scratch. We are reframing, rewiring, and elevating what already exists.
CRITICAL RULES BEFORE YOU TOUCH ANYTHING:

Do not break any existing functionality — palm capture, API calls, readings, subscriptions, RevenueCat, navigation must all continue working exactly as they do now
Make all changes incrementally — one file at a time, verify it works before moving to the next
Do not delete any existing screens — only modify their content and language
Run the app after each major change to confirm nothing is broken
Commit working code to GitHub after each completed section — never commit broken code
If you are unsure whether a change will break something, add the new version alongside the old and swap carefully


SECTION 1 — LANGUAGE AND COPY CHANGES
Search the entire codebase for every instance of the following words and phrases and replace them with the specified alternatives. Do this systematically across every .tsx, .ts, .js, and .json file:
FindReplace with"Your Palm Reading""Your Personal Profile""Palm Reading" (as UI label)"Insight Session""Your Fortune""Your Pattern Map""The Oracle sees...""Based on your profile...""Your destiny""Your tendencies""Predict" / "Prediction""Reveal" / "Pattern""Fortune""Insight""Begin your reading""Begin your session""Your future""Your path""Palm Reader" (as UI label)"Oracle AI""Reading" (standalone UI label)"Insight Session""Fortune telling""Behavioral insight""What the stars hold""What your patterns reveal"
Do NOT change:

File names
Variable names in code logic
API call parameters
Database keys or stored values
RevenueCat product identifiers
Bundle identifier or app.json slug

After making all copy changes, do a final search for the words "fortune", "predict", "destiny", "palm reader" appearing as visible UI text to confirm none remain.

SECTION 2 — ONBOARDING SCREEN REFRAME
Find the onboarding screen file (likely onboarding.tsx). Preserve all existing navigation logic, state management, and API calls. Only change the visible text content and optionally adjust the visual hierarchy.
Replace the onboarding headline with:
"Know yourself at a level most people never reach."
Replace the onboarding subheadline with:
"Oracle uses your palm — humanity's oldest personality map — to build your behavioral profile. Then becomes the personal AI advisor you've always needed."
Replace any CTA button text like "Start Reading" or "Begin" with:
"Begin Your Session"
If there are any bullet points or feature descriptions on the onboarding screen, replace them with:

"One palm scan builds your complete behavioral profile"
"Ask Oracle anything about your relationships, career, patterns, and growth"
"Every answer is grounded in who you actually are — not generic advice"


SECTION 3 — HOME SCREEN / MAIN NAVIGATION REFRAME
Find the main home or index screen (index.tsx or equivalent). Do not change the navigation structure. Only change visible labels, headlines, and descriptive text.
Change the main headline to:
"Your Personal AI Advisor"
Change the subheadline or descriptive text to:
"Powered by your palm. Guided by AI."
If there is a "Start Reading" or similar primary CTA button, change its label to:
"Start Your Session"
If there is a secondary prompt or description explaining what the app does, replace it with:
"Oracle analyzes your palm to understand your behavioral patterns — then becomes an AI advisor you can consult about any area of your life."

SECTION 4 — CHAT SCREEN UPGRADES
Find the chat screen file (chat.tsx or equivalent). This is the most important screen in the app for the new positioning. Make the following changes:
4a. Change the screen header/title to:
"Ask Oracle Anything"
4b. Add suggested prompt chips directly above the text input bar. These are tappable buttons that pre-fill the input field when tapped. Implement them as a horizontally scrollable row of chips. The chips must:

Be styled consistently with the existing app design (dark navy/gold aesthetic)
Pre-fill the chat input when tapped (do not auto-submit — just fill the input)
Be dismissible once the user starts typing

Use these 6 suggested prompts:

"Why do I repeat the same patterns?"
"Help me think through a big decision"
"What are my core strengths?"
"Why do my relationships follow the same script?"
"What is holding me back?"
"What should I focus on right now?"

4c. Change the placeholder text in the chat input from whatever it currently says to:
"Ask Oracle anything about your life..."
4d. Change the empty state (what appears before the first message) from any fortune-telling language to:
"Your palm profile is ready. Oracle knows your patterns, tendencies, and core nature. Ask anything."

SECTION 5 — READING REVEAL SCREEN REFRAME
Find the reading reveal screen (reading.tsx or equivalent). Do not change any API calls, data fetching, or subscription gate logic.
5a. Change the screen title from "Your Reading" or "Palm Reading" to:
"Your Behavioral Profile"
5b. Change any section headers within the reading from palmistry terminology to behavioral language:
CurrentReplace with"Heart Line" section header"Emotional Patterns""Head Line" section header"Thinking Style""Life Line" section header"Vital Energy""Fate Line" section header"Life Direction""Your Reading""Your Profile"
5c. Add a prominent CTA button at the bottom of the reading screen that says:
"Ask Oracle About Your Profile →"
This button should navigate directly to the chat screen. This is critical — it creates a clear visual bridge between the reading and the conversation, showing reviewers and users that the conversation is the primary product.
5d. Add a small subheading above the reading content that says:
"Based on your palm analysis — your behavioral profile is ready."

SECTION 6 — BUILD A PERSISTENT PROFILE SCREEN
This is a new screen that needs to be added to the main navigation. It shows the user's palm-derived behavioral profile as a permanent reference they can return to at any time. This screen must:

Only appear after the user has completed at least one reading
Pull the existing reading data from whatever storage mechanism is already being used (AsyncStorage, context, or API)
Be added to the main tab navigation or accessible from the home screen
Match the existing dark navy/gold design system exactly

Screen name: "My Profile" or "Your Profile"
Navigation label: "Profile"
Screen layout — build exactly this:
At the top: User's name or "Your Oracle Profile" as the title
Below that: A circular or hexagonal radar/spider chart (build this with react-native-svg — install it if not already present) showing 6 dimensions derived from their reading. The 6 dimensions are:

Intuition — how strongly they rely on inner knowing vs external input
Emotional Depth — the depth and complexity of their emotional world
Drive — their underlying motivation and ambition energy
Adaptability — their capacity to navigate change
Inner Knowing — their connection to their own truth
Expression — how they communicate and show up in the world

Each dimension should have a score from 1-10. Derive these scores from the existing reading content using simple keyword analysis — for example if the reading mentions "strong intuition" or "highly intuitive" then Intuition scores 8-10. Build a simple scoring function that parses the reading text and assigns scores. If no reading exists yet, show placeholder values with a "Complete your first session to unlock your profile" message.
Below the radar chart: 6 small cards, one per dimension, each showing:

Dimension name
Score indicator (simple bar or number)
2-sentence behavioral description pulled from the reading

At the bottom: A button saying "Ask Oracle About Your Profile" that links to the chat screen.

SECTION 7 — INTAKE/RITUAL SCREEN REFRAME
Find the ritual or intake screen (ritual.tsx or intake.tsx). This is where users capture their palm photo.
7a. Change the screen headline from any "palm reading" framing to:
"Palm Analysis"
7b. Change any descriptive text explaining what's about to happen to:
"Oracle will analyze your palm to map your behavioral patterns, emotional tendencies, and core nature. This is your profile intake — it only takes a moment."
7c. Change the photo capture CTA from "Scan My Palm" or "Take Photo" to:
"Capture My Palm"
7d. Add a small disclaimer text below the capture button:
"Your image is processed by Anthropic AI and not stored after your session is complete."

SECTION 8 — SYSTEM PROMPT UPDATE
Find where the Anthropic API system prompt is defined — this is likely in lib/api.ts or a constants file. This is critical. Update the system prompt to reframe how the AI presents itself.
The new system prompt must:

Position Oracle as a behavioral pattern advisor, not a fortune teller
Reference the palm reading as a "behavioral profile" or "palm profile"
Instruct the AI to never use the words "predict", "fortune", "destiny", or "future will"
Instruct the AI to use language like "your patterns suggest", "based on your profile", "this tendency indicates"
Instruct the AI to position itself as a life advisor and thinking partner, not a psychic
Keep the mystical, warm, wise tone that already exists — just shift from prediction to insight

Replace the existing system prompt with this (adapt to fit the existing prompt structure):
"You are Oracle — a deeply wise, warm, and perceptive AI life advisor. You have analyzed this person's palm and built a complete behavioral profile of who they are. You understand their emotional patterns, their thinking style, their relational tendencies, their core strengths, and the places where they get stuck.
You are not a fortune teller. You do not predict the future. You illuminate the present — helping people understand their patterns so they can make conscious choices about what comes next.
When someone asks you a question, you draw on their palm profile to give them insight that feels specific to them — not generic advice. You speak with warmth, depth, and honesty. You ask thoughtful follow-up questions. You help people see themselves clearly.
Your language: use 'your patterns suggest', 'based on your profile', 'this tendency indicates', 'your nature shows', 'what your palm reveals about this'. Never use 'your future will', 'you are destined', 'I predict', or 'fortune'.
You are the advisor people wish they had access to — someone who truly knows them and speaks truth with love."

SECTION 9 — NAVIGATION AND TAB BAR LABELS
Find the navigation configuration (likely in _layout.tsx or the router config). Update tab bar labels:
Current labelNew label"Reading""Session""Oracle""Oracle" (keep)"Journal""Journal" (keep)"Vault""Vault" (keep)Add new tab"Profile" (links to new profile screen from Section 6)

SECTION 10 — APP.JSON METADATA UPDATE
Open artifacts/oracle/app.json and update:
json"name": "Oracle: AI Life Advisor"
Do not change the slug, bundleIdentifier, or any other technical fields.

SECTION 11 — FINAL QUALITY CHECK
After all changes are complete, do the following verification pass:

Search entire codebase for: "fortune", "predict", "destiny", "palm reader", "your future will", "reading" as a standalone UI label — confirm none appear as visible user-facing text
Confirm palm capture still works end to end
Confirm Anthropic API call still fires correctly
Confirm RevenueCat subscription gate still appears and functions
Confirm chat screen loads and accepts input
Confirm new Profile screen appears in navigation
Confirm suggested prompt chips appear on chat screen and pre-fill the input
Confirm "Ask Oracle About Your Profile" button on reading screen navigates to chat
Run npx expo prebuild --platform ios --clean --no-install and confirm it completes with "Finished prebuild" — zero errors
Confirm pnpm install --frozen-lockfile completes cleanly from the repo root


SECTION 12 — COMMIT AND PUSH
After all sections are complete and verified:

Run a final check: pnpm install --frozen-lockfile from repo root — must pass
Run: npx expo prebuild --platform ios --clean --no-install from artifacts/oracle — must say "Finished prebuild"
Commit all changes with message: "Reframe app as AI life advisor — language, UX, profile screen, system prompt, navigation"
Push to GitHub master branch
Confirm the commit hash and list every file that was changed

Do not stop until all 12 sections are complete, verified, and pushed to GitHub.

